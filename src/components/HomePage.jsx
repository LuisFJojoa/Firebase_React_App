import React from 'react'

const HomePage = () => {
    return (
        <div>
            <h2>Home Page (Currently there is no info here). Please go to the product window to create a new product</h2>
        </div>
    )
}

export default HomePage
